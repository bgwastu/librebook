name: librebook
description: An ebook downloader.
version: 0.0.1-alpha

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3

  # architecture
  get:

  # hooks
  flutter_hooks:
  stacked_hooks:
  
  # injection
  get_it:
  
  # api
  http:
  html:

  #misc
  shimmer:
  cached_network_image:
  async:
  enhanced_future_builder:
  lazy_load_refresh_indicator:
  flutter_statusbarcolor:
  outline_material_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
